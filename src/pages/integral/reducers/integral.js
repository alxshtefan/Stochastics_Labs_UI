/* eslint-disable no-param-reassign,no-plusplus */

const initialState = {

};

export const integral = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state
  }
};
